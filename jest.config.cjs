module.exports = {
    testEnvironment: 'jest-environment-jsdom',
    setupFiles: ['./jest.setup.js'],
    //en esta configuracion especificamos patrones que queremos ignorar
    transformIgnorePatterns: []
}